version: 2

seeds:
  - name: fullstory_events_types
    description: Classifiers for the types of event.
    columns:
      - name: name
        tests:
          - unique
          - not_null
      - name: description

  - name: fullstory_source_types
    description: Classifiers for the originating source of events.
    columns:
      - name: name
        tests:
          - unique
          - not_null
      - name: description
